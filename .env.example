# Example environment variables for local development and documentation
# Copy to `.env.local` and update values for your environment

# Base URL for your backend API. Used in client-side code as `import.meta.env.VITE_API_URL`.
VITE_API_URL=http://localhost:8000
